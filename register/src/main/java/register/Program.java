package register;


/**
 * The program which starts the whole GUI
 */
public class Program {
    /**
     * Main function which runs the GUI
     * @param args if no input is givenit will be taken from the config file
     */
    public static void main(String[] args) {
        // General interface settings
        GUI.run();
    }
}